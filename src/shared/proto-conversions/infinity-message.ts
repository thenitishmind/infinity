// Temporary stub for infinity-message.ts - normally this would contain proto conversion functions

export const convertInfinityMessage = (message: any) => {
  // This would normally contain conversion logic
  return message
}

export const convertClineMessageToProto = (message: any) => {
  // This would normally contain conversion logic for Cline messages
  return message
}